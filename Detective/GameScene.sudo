# GameScene

GameScene {
  characters = [Character] 
  environment :length=vey details
  plot:length=vey details // the story for this scene
  Place
  
  Constraints {
    if any of 'Characters' is new, need to describe this character in details
    Authors to emulate:  柯南道尔
  }
}
